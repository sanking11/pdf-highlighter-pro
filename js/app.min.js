
/*! PDF Bulk Highlighter - Protected Code */
(function(_0xwindow,_0xdocument){
'use strict';

(function(){var _0xcheck=function(){var _0xstart=new Date();
debugger;var _0xend=new Date();
if(_0xend-_0xstart>100){window.location.reload();}};
setInterval(_0xcheck,1000);})();
let highlightRules=[];let uploadedFiles=[];let processedFiles=[];let editingRuleId=null;let detailedReport=[];let lifetimeFilesProcessed=0;let globalFilesProcessed=0;const SUPABASE_URL='https://zeyjrzhucptfstjpucht.supabase.co';const SUPABASE_ANON_KEY='eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InpleWpyemh1Y3B0ZnN0anB1Y2h0Iiwicm9sZSI6ImFub24iLCJpYXQiOjE3NjM1MzM5NTgsImV4cCI6MjA3OTEwOTk1OH0.f3zVK1HY_RZcsk3Kgktr8m-mmnqFroJVOu2Z05OxkNE';const{createClient}=window.supabase;const supabaseClient=createClient(SUPABASE_URL,SUPABASE_ANON_KEY);if(typeof pdfjsLib!=='undefined'){pdfjsLib.GlobalWorkerOptions.workerSrc='https://cdnjs.cloudflare.com/ajax/libs/pdf.js/3.11.174/pdf.worker.min.js'}let ocrWorker=null;async function initOCRWorker(){if(ocrWorker)return ocrWorker;try{ocrWorker=await Tesseract.createWorker('eng',1,{logger:m=>{if(m.status==='recognizing text'){console.log(`OCR Progress: ${Math.round(m.progress * 100)}%`)}}});console.log('OCR Worker initialized successfully');return ocrWorker}catch(error){console.error('Error initializing OCR worker:',error);return null}}async function hasExtractableText(pdfPage){try{const textContent=await pdfPage.getTextContent();const textItems=textContent.items.filter(item=>item.str&&item.str.trim().length>0);return textItems.length>5}catch(error){console.error('Error checking text content:',error);return false}}async function performOCR(pdfPage,pdfPageDimensions){try{const worker=await initOCRWorker();if(!worker){throw new Error('OCR worker not available')}const scale=2.0;const viewport=pdfPage.getViewport({scale:scale});const canvas=document.createElement('canvas');const context=canvas.getContext('2d');canvas.height=viewport.height;canvas.width=viewport.width;const renderContext={canvasContext:context,viewport:viewport};await pdfPage.render(renderContext).promise;const{data}=await worker.recognize(canvas);const textItems=[];if(data.words&&data.words.length>0){data.words.forEach(word=>{if(word.text&&word.text.trim().length>0){const x=word.bbox.x0/scale;const width=(word.bbox.x1-word.bbox.x0)/scale;const height=(word.bbox.y1-word.bbox.y0)/scale;const y=pdfPageDimensions.height-(word.bbox.y1/scale);const fontSize=height;textItems.push({str:word.text,transform:[fontSize,0,0,fontSize,x,y],width:width,height:height,fromOCR:true})}})}return{items:textItems}}catch(error){console.error('OCR Error:',error);return{items:[]}}}function loadSavedRules(){try{const savedRules=localStorage.getItem('pdfHighlighterRules');if(savedRules){highlightRules=JSON.parse(savedRules);displayRules();updateProcessButton();console.log('Loaded',highlightRules.length,'saved rules')}}catch(error){console.error('Error loading saved rules:',error)}}function loadLifetimeCount(){try{const savedCount=localStorage.getItem('pdfHighlighterLifetimeCount');if(savedCount){lifetimeFilesProcessed=parseInt(savedCount)||0;console.log('Loaded lifetime count:',lifetimeFilesProcessed,'files processed')}}catch(error){console.error('Error loading lifetime count:',error)}}function saveLifetimeCount(){try{localStorage.setItem('pdfHighlighterLifetimeCount',lifetimeFilesProcessed.toString());console.log('Saved lifetime count:',lifetimeFilesProcessed)}catch(error){console.error('Error saving lifetime count:',error)}}async function fetchGlobalCount(){try{console.log('Fetching global count from Supabase...');const{data,error}=await supabaseClient .from('global_stats').select('total_files_processed').eq('id',1).single();if(error){console.error('Error fetching global count:',error);console.log('This may be because the global_stats table does not exist or is not accessible');return 0}globalFilesProcessed=data.total_files_processed||0;console.log('✓ Successfully fetched global count:',globalFilesProcessed);return globalFilesProcessed}catch(error){console.error('Error fetching global count:',error);console.log('Global counter will remain at 0');return 0}}async function updateGlobalCount(filesCount){try{console.log(`Updating global count (adding ${filesCount} files)...`);const{data:currentData,error:fetchError}=await supabaseClient .from('global_stats').select('total_files_processed').eq('id',1).single();if(fetchError){console.error('Error fetching current global count:',fetchError);console.log('Unable to update global statistics - database may not be configured');return false}const currentTotal=currentData.total_files_processed||0;const newTotal=currentTotal+filesCount;console.log(`Current global total: ${currentTotal}, New total: ${newTotal}`);const{error:updateError}=await supabaseClient .from('global_stats').update({total_files_processed:newTotal,last_updated:new Date().toISOString()}).eq('id',1);if(updateError){console.error('Error updating global count:',updateError);return false}globalFilesProcessed=newTotal;console.log('✓ Successfully updated global count to:',globalFilesProcessed);return true}catch(error){console.error('Error updating global count:',error);console.log('Global counter update failed - continuing without it');return false}}function saveRulesToStorage(){try{localStorage.setItem('pdfHighlighterRules',JSON.stringify(highlightRules));console.log('Saved',highlightRules.length,'rules to storage')}catch(error){console.error('Error saving rules:',error)}}function editRule(id){const rule=highlightRules.find(r=>r.id===id);if(!rule)return;document.getElementById('wordInput').value=rule.word;document.getElementById('colorInput').value=rule.color;document.getElementById('opacityInput').value=rule.opacity;document.getElementById('exactMatchInput').checked=rule.exactMatch;const addBtn=document.getElementById('addRuleBtn');addBtn.textContent='Update Rule';addBtn.style.background='linear-gradient(135deg, #00aa00, #00dd00)';document.getElementById('cancelEditBtn').style.display='inline-block';editingRuleId=id;window.scrollTo({top:0,behavior:'smooth'})}function cancelEdit(){editingRuleId=null;const addBtn=document.getElementById('addRuleBtn');addBtn.textContent='Add Rule';addBtn.style.background='linear-gradient(135deg, #ff6b00, #ff9933)';document.getElementById('cancelEditBtn').style.display='none';document.getElementById('wordInput').value='';document.getElementById('colorInput').value='#ffff00';document.getElementById('opacityInput').value='0.3';document.getElementById('exactMatchInput').checked=false}function addRule(){const word=document.getElementById('wordInput').value.trim();const color=document.getElementById('colorInput').value;const opacity=parseFloat(document.getElementById('opacityInput').value);const exactMatch=document.getElementById('exactMatchInput').checked;if(!word){alert('Please enter a word or phrase to highlight');return}if(opacity<0||opacity>1){alert('Opacity must be between 0 and 1');return}if(editingRuleId!==null){const ruleIndex=highlightRules.findIndex(r=>r.id===editingRuleId);if(ruleIndex!==-1){highlightRules[ruleIndex]={id:editingRuleId,word:word,color:color,opacity:opacity,exactMatch:exactMatch};saveRulesToStorage();displayRules();cancelEdit()}}else{const newRule={id:Date.now(),word:word,color:color,opacity:opacity,exactMatch:exactMatch};highlightRules.push(newRule);saveRulesToStorage();displayRules();document.getElementById('wordInput').value='';document.getElementById('colorInput').value='#ffff00';document.getElementById('opacityInput').value='0.3';document.getElementById('exactMatchInput').checked=false}updateProcessButton()}function displayRules(){const rulesList=document.getElementById('rulesList');const clearAllBtn=document.getElementById('clearAllBtn');const exportBtn=document.getElementById('exportRulesBtn');rulesList.innerHTML='';if(highlightRules.length===0){rulesList.innerHTML='<p style="text-align: center; color: #999;">No rules added yet. Add words to highlight.</p>';clearAllBtn.style.display='none';exportBtn.style.display='none';return}clearAllBtn.style.display='inline-block';exportBtn.style.display='inline-block';highlightRules.forEach(rule=>{const ruleItem=document.createElement('div');ruleItem.className='rule-item';const matchType=rule.exactMatch?'(Exact)':'(Partial)';ruleItem.innerHTML=`<div class="rule-info"><div class="color-preview" style="background-color: ${rule.color}; opacity: ${rule.opacity};"></div><span class="rule-text"><strong>${rule.word}</strong> ${matchType} - Opacity: ${rule.opacity}</span></div><div style="display: flex; gap: 10px;"><button class="btn-edit" data-rule-id="${rule.id}">Edit</button><button class="btn-remove" data-rule-id="${rule.id}">Remove</button></div>`;rulesList.appendChild(ruleItem)});document.querySelectorAll('.btn-edit').forEach(btn=>{btn.addEventListener('click',function(){const ruleId=parseInt(this.getAttribute('data-rule-id'));editRule(ruleId)})});document.querySelectorAll('.btn-remove').forEach(btn=>{btn.addEventListener('click',function(){const ruleId=parseInt(this.getAttribute('data-rule-id'));removeRule(ruleId)})})}function removeRule(ruleId){const ruleIndex=highlightRules.findIndex(r=>r.id===ruleId);if(ruleIndex!==-1){highlightRules.splice(ruleIndex,1);displayRules();updateProcessButton();saveRulesToStorage();if(highlightRules.length===0){clearProgressCard()}}}function clearAllRules(){if(confirm('Are you sure you want to clear all highlighting rules?')){highlightRules=[];displayRules();updateProcessButton();saveRulesToStorage();clearProgressCard()}}function exportRules(){if(highlightRules.length===0){alert('No rules to export. Please add some rules first.');return}const dataStr=JSON.stringify(highlightRules,null,2);const dataBlob=new Blob([dataStr],{type:'application/json'});const url=URL.createObjectURL(dataBlob);const link=document.createElement('a');link.href=url;link.download=`pdf-highlighter-rules-${Date.now()}.json`;document.body.appendChild(link);link.click();document.body.removeChild(link);URL.revokeObjectURL(url);console.log('Exported',highlightRules.length,'rules');const exportBtn=document.getElementById('exportRulesBtn');const originalText=exportBtn.textContent;exportBtn.textContent='✓ Exported!';exportBtn.style.background='rgba(0, 200, 0, 0.3)';setTimeout(()=>{exportBtn.textContent=originalText;exportBtn.style.background='rgba(100, 200, 100, 0.15)'},2000)}function importRules(event){const file=event.target.files[0];if(!file)return;const reader=new FileReader();reader.onload=function(e){try{const importedRules=JSON.parse(e.target.result);if(!Array.isArray(importedRules)){alert('Invalid file format. Expected an array of rules.');return}const isValid=importedRules.every(rule=>rule.hasOwnProperty('word')&&rule.hasOwnProperty('color')&&rule.hasOwnProperty('opacity'));if(!isValid){alert('Invalid rule format in the file.');return}let shouldReplace=true;if(highlightRules.length>0){shouldReplace=confirm(`You have ${highlightRules.length} existing rule(s).\n\nClick OK to REPLACE all rules.\nClick Cancel to MERGE (add to existing rules).`)}if(shouldReplace){highlightRules=importedRules}else{importedRules.forEach(rule=>{highlightRules.push({...rule,id:Date.now()+Math.random()})})}displayRules();updateProcessButton();saveRulesToStorage();alert(`Successfully imported ${importedRules.length} rule(s)!`);console.log('Imported',importedRules.length,'rules')}catch(error){console.error('Import error:',error);alert('Error reading file. Please ensure it\'s a valid JSON file exported from this tool.')}event.target.value=''};reader.readAsText(file)}function removeFile(index){if(index>=0&&index<uploadedFiles.length){uploadedFiles.splice(index,1);displayFiles();updateProcessButton();if(uploadedFiles.length===0){document.querySelector('label[for="pdfFiles"]').classList.remove('has-files');document.querySelector('label[for="zipFile"]').classList.remove('has-files')}document.getElementById('pdfFiles').value='';document.getElementById('zipFile').value=''}}function handlePDFUpload(event){const files=Array.from(event.target.files);if(files.length===0){console.log('No files selected');return}let addedCount=0;files.forEach(file=>{console.log('Checking file:',file.name,'Size:',file.size,'Type:',file.type);if(file.type==='application/pdf'){const isDuplicate=uploadedFiles.some(existingFile=>{const match=existingFile.name===file.name&&existingFile.size===file.size;if(match){console.log('Duplicate found:',file.name)}return match});if(!isDuplicate){uploadedFiles.push(file);addedCount++;console.log('Added file:',file.name)}}else{console.log('Not a PDF, skipping:',file.name)}});console.log(`Total added: ${addedCount}, Total files now: ${uploadedFiles.length}`);displayFiles();updateProcessButton();if(uploadedFiles.length>0){document.querySelector('label[for="pdfFiles"]').classList.add('has-files')}event.target.value=''}async function handleZIPUpload(event){const file=event.target.files[0];if(!file)return;console.log('Loading ZIP file:',file.name);try{const zip=await JSZip.loadAsync(file);let pdfCount=0;let duplicateCount=0;for(const [filename,zipEntry] of Object.entries(zip.files)){if(!zipEntry.dir&&filename.toLowerCase().endsWith('.pdf')){const blob=await zipEntry.async('blob');const pdfFile=new File([blob],filename,{type:'application/pdf'});console.log('Checking ZIP file:',pdfFile.name,'Size:',pdfFile.size);const isDuplicate=uploadedFiles.some(existingFile=>existingFile.name===pdfFile.name&&existingFile.size===pdfFile.size);if(!isDuplicate){uploadedFiles.push(pdfFile);pdfCount++;console.log('Added from ZIP:',pdfFile.name)}else{duplicateCount++;console.log('Duplicate in ZIP:',pdfFile.name)}}}console.log(`Extracted ${pdfCount} PDFs, skipped ${duplicateCount} duplicates. Total files: ${uploadedFiles.length}`);displayFiles();updateProcessButton();if(uploadedFiles.length>0){document.querySelector('label[for="zipFile"]').classList.add('has-files')}}catch(error){console.error('Error reading ZIP file:',error);alert('Error reading ZIP file. Please ensure it contains valid PDF files.')}event.target.value=''}function displayFiles(){const filesList=document.getElementById('filesList');const clearAllFilesBtn=document.getElementById('clearAllFilesBtn');filesList.innerHTML='';if(uploadedFiles.length===0){filesList.innerHTML='<p style="text-align: center; color: #999;">No files uploaded yet.</p>';clearAllFilesBtn.style.display='none';return}clearAllFilesBtn.style.display='inline-block';uploadedFiles.forEach((file,index)=>{const fileItem=document.createElement('div');fileItem.className='file-item';fileItem.style.display='flex';fileItem.style.justifyContent='space-between';fileItem.style.alignItems='center';const fileInfo=document.createElement('span');fileInfo.textContent=`${index + 1}. ${file.name} (${formatFileSize(file.size)})`;const removeBtn=document.createElement('button');removeBtn.className='btn-remove-file';removeBtn.textContent='Remove';removeBtn.setAttribute('data-file-index',index);fileItem.appendChild(fileInfo);fileItem.appendChild(removeBtn);filesList.appendChild(fileItem)});document.querySelectorAll('.btn-remove-file').forEach(btn=>{btn.addEventListener('click',function(){const fileIndex=parseInt(this.getAttribute('data-file-index'));removeFile(fileIndex)})})}function removeFile(index){if(index>=0&&index<uploadedFiles.length){uploadedFiles.splice(index,1);displayFiles();updateProcessButton();clearProgressCard();if(uploadedFiles.length===0){document.querySelector('label[for="pdfFiles"]').classList.remove('has-files');document.querySelector('label[for="zipFile"]').classList.remove('has-files')}}}function clearProgressCard(){document.getElementById('progressCard').style.display='none';document.getElementById('progressFill').style.width='0%';document.getElementById('progressText').textContent='0 / 0 files processed';document.getElementById('processingLog').innerHTML='';const dashboardCard=document.getElementById('dashboardCard');if(dashboardCard){dashboardCard.style.display='none'}const downloadBtn=document.getElementById('downloadBtn');if(downloadBtn){downloadBtn.classList.remove('show');downloadBtn.disabled=true}}function clearAllFiles(){if(uploadedFiles.length===0)return;if(confirm(`Are you sure you want to remove all ${uploadedFiles.length} file(s)?`)){uploadedFiles=[];displayFiles();updateProcessButton();clearProgressCard();document.querySelector('label[for="pdfFiles"]').classList.remove('has-files');document.querySelector('label[for="zipFile"]').classList.remove('has-files');document.getElementById('pdfFiles').value='';document.getElementById('zipFile').value=''}}function formatFileSize(bytes){if(bytes===0)return '0 Bytes';const k=1024;const sizes=['Bytes','KB','MB','GB'];const i=Math.floor(Math.log(bytes)/Math.log(k));return Math.round(bytes/Math.pow(k,i)*100)/100+' '+sizes[i]}function updateProcessButton(){const processBtn=document.getElementById('processBtn');processBtn.disabled=!(uploadedFiles.length>0&&highlightRules.length>0)}async function processFiles(){if(uploadedFiles.length===0||highlightRules.length===0){alert('Please add highlighting rules and upload PDF files');return}processedFiles=[];detailedReport=[];const startTime=Date.now();let totalHighlightsApplied=0;let totalWordsSearched=highlightRules.length;let filesWithHighlights=0;document.getElementById('progressCard').style.display='block';document.getElementById('processBtn').disabled=true;const downloadBtn=document.getElementById('downloadBtn');downloadBtn.classList.remove('show');downloadBtn.disabled=true;const totalFiles=uploadedFiles.length;let processedCount=0;addLog('info',`Starting processing of ${totalFiles} files...`);for(const file of uploadedFiles){try{addLog('info',`Processing: ${file.name}...`);const result=await highlightPDF(file);processedFiles.push({name:file.name,data:result.pdfBytes});totalHighlightsApplied+=result.highlightCount;if(result.highlightCount>0){filesWithHighlights++}detailedReport.push({filename:file.name,pages:result.pagesInfo,termDetails:result.termDetails});addLog('success',`✓ Completed: ${file.name}`)}catch(error){addLog('error',`✗ Failed: ${file.name} - ${error.message}`)}processedCount++;updateProgress(processedCount,totalFiles)}const endTime=Date.now();const timeTaken=((endTime-startTime)/1000).toFixed(2);lifetimeFilesProcessed+=processedFiles.length;saveLifetimeCount();addLog('info','Updating global statistics...');const updateSuccess=await updateGlobalCount(processedFiles.length);if(!updateSuccess){addLog('info','Note: Global statistics update failed, but your files are processed successfully.')}addLog('success',`Processing complete! ${processedFiles.length} of ${totalFiles} files successful.`);addDashboard({totalFiles:totalFiles,successfulFiles:processedFiles.length,failedFiles:totalFiles-processedFiles.length,totalWordsSearched:totalWordsSearched,totalHighlights:totalHighlightsApplied,filesWithHighlights:filesWithHighlights,timeTaken:timeTaken,lifetimeTotal:lifetimeFilesProcessed,globalTotal:globalFilesProcessed});downloadBtn.classList.add('show');downloadBtn.disabled=false;document.getElementById('processBtn').disabled=false;addLog('info','✓ Ready to download! Click the GREEN download button below.')}async function highlightPDF(file){try{const arrayBuffer=await file.arrayBuffer();const pdfDoc=await PDFLib.PDFDocument.load(arrayBuffer);const pages=pdfDoc.getPages();addLog('info',`  Loaded PDF with ${pages.length} pages`);const loadingTask=pdfjsLib.getDocument({data:arrayBuffer});const pdfDocument=await loadingTask.promise;let totalHighlights=0;const highlightedItems=new Set();const termStats={};highlightRules.forEach(rule=>{termStats[rule.word]={term:rule.word,color:rule.color,matchType:rule.exactMatch ? 'Whole Word':'Partial',caseSensitive:'No',count:0,pages:[]}});const pagesWithHighlights=new Set();for(let pageIndex=0;pageIndex<pages.length;pageIndex++){const page=pages[pageIndex];const{width,height}=page.getSize();const pdfPage=await pdfDocument.getPage(pageIndex+1);const hasText=await hasExtractableText(pdfPage);let textContent;if(!hasText){addLog('warning',`  Page ${pageIndex + 1}: No text layer detected, using OCR...`);const pageDimensions={width,height};textContent=await performOCR(pdfPage,pageDimensions);if(textContent.items.length>0){addLog('success',`  Page ${pageIndex + 1}: OCR completed, found ${textContent.items.length} text items`)}else{addLog('warning',`  Page ${pageIndex + 1}: OCR found no text`)}}else{textContent=await pdfPage.getTextContent()}const textItems=textContent.items.filter(item=>item.str);textItems.sort((a,b)=>{const yDiff=Math.abs(a.transform[5]-b.transform[5]);if(yDiff>5){return b.transform[5]-a.transform[5]}return a.transform[4]-b.transform[4]});for(const rule of highlightRules){const searchText=rule.word.toLowerCase().replace(/\s+/g,'').replace(/-/g,'');if(rule.exactMatch){for(let i=0;i<textItems.length;i++){const item=textItems[i];const itemKey=`${pageIndex}-${i}`;if(highlightedItems.has(itemKey))continue;const text=item.str.toLowerCase().trim().replace(/\s+/g,'').replace(/-/g,'');if(text===searchText){highlightEntireItem(page,item,height,rule);highlightedItems.add(itemKey);totalHighlights++;termStats[rule.word].count++;if(!termStats[rule.word].pages.includes(`p${pageIndex + 1}`)){termStats[rule.word].pages.push(`p${pageIndex + 1}`)}pagesWithHighlights.add(pageIndex+1);continue}if(searchText.startsWith(text)&&text.length>0&&i<textItems.length-1){let combinedText=text;let itemsToHighlight=[i];for(let j=i+1;j<Math.min(i+10,textItems.length);j++){const nextItem=textItems[j];const nextItemKey=`${pageIndex}-${j}`;if(highlightedItems.has(nextItemKey))break;const nextText=nextItem.str.toLowerCase().trim().replace(/\s+/g,'').replace(/-/g,'');const yDiff=Math.abs(item.transform[5]-nextItem.transform[5]);const xDiff=nextItem.transform[4]-(item.transform[4]+item.width);if(yDiff<5&&xDiff<100){const testCombined=combinedText+nextText;if(searchText===testCombined){itemsToHighlight.push(j);itemsToHighlight.forEach(idx=>{highlightEntireItem(page,textItems[idx],height,rule);highlightedItems.add(`${pageIndex}-${idx}`)});totalHighlights++;termStats[rule.word].count++;if(!termStats[rule.word].pages.includes(`p${pageIndex + 1}`)){termStats[rule.word].pages.push(`p${pageIndex + 1}`)}pagesWithHighlights.add(pageIndex+1);break}else if(searchText.startsWith(testCombined)&&nextText.length>0){combinedText=testCombined;itemsToHighlight.push(j)}else{break}}else{break}}}}}else{textItems.forEach((item,index)=>{const itemKey=`${pageIndex}-${index}-${rule.id}`;const text=item.str.toLowerCase();const searchLower=rule.word.toLowerCase();if(text.includes(searchLower)){highlightWordInItem(page,item,height,rule,searchLower);totalHighlights++;termStats[rule.word].count++;if(!termStats[rule.word].pages.includes(`p${pageIndex + 1}`)){termStats[rule.word].pages.push(`p${pageIndex + 1}`)}pagesWithHighlights.add(pageIndex+1)}})}}}function highlightEntireItem(page,item,height,rule){const transform=item.transform;const x=transform[4];const y=transform[5];const fullWidth=item.width||50;const a=transform[0];const b=transform[1];const angleRad=Math.atan2(b,a);const angleDeg=angleRad*180/Math.PI;const fontSize=Math.sqrt(a*a+b*b);const textHeight=fontSize;const rgb=hexToRgb(rule.color);try{page.drawRectangle({x:x-1,y:y-2,width:fullWidth+2,height:textHeight+4,rotate:PDFLib.degrees(angleDeg),color:PDFLib.rgb(rgb.r/255,rgb.g/255,rgb.b/255),opacity:rule.opacity,borderWidth:0})}catch(drawError){console.error('Error drawing entire item highlight:',drawError)}}function highlightWordInItem(page,item,height,rule,searchWord){const transform=item.transform;const x=transform[4];const y=transform[5];const fullText=item.str;const fullWidth=item.width||50;const a=transform[0];const b=transform[1];const angleRad=Math.atan2(b,a);const angleDeg=angleRad*180/Math.PI;const fontSize=Math.sqrt(a*a+b*b);const textHeight=fontSize;const rgb=hexToRgb(rule.color);const lowerText=fullText.toLowerCase();let startIndex=0;while((startIndex=lowerText.indexOf(searchWord,startIndex))!==-1){const matchLength=searchWord.length;let estimatedOffset=0;let estimatedWidth=0;for(let i=0;i<fullText.length;i++){const char=fullText[i];let charWeight=1.0;if(char===' ')charWeight=0.3;else if('iIl|!.,;:\'"'.includes(char)) charWeight = 0.4;
                            else if ('fjtJ()[]{}`.'.includes(char)) charWeight = 0.5;
                            else if ('rT-+=*^~'.includes(char)) charWeight = 0.6;
                            else if ('MWmw@%#'.includes(char)) charWeight = 1.3;
                            else if (char === char.toUpperCase() && char !== char.toLowerCase()) charWeight = 1.1;
                            
                            if (i < startIndex) {
                                estimatedOffset += charWeight;
                            } else if (i < startIndex + matchLength) {
                                estimatedWidth += charWeight;
                            }
                        }
                        
                        // Calculate total weight
                        let totalWeight = 0;
                        for (let i = 0; i < fullText.length; i++) {
                            const char = fullText[i];
                            let charWeight = 1.0;
                            if (char === ' ') charWeight = 0.3;
                            else if ('iIl|!.,;:\'"'.includes(char))charWeight=0.4;else if('fjtJ()[]{}`.'.includes(char))charWeight=0.5;else if('rT-+=*^~'.includes(char))charWeight=0.6;else if('MWmw@%#'.includes(char))charWeight=1.3;else if(char===char.toUpperCase()&&char!==char.toLowerCase())charWeight=1.1;totalWeight+=charWeight}const offsetX=(estimatedOffset/totalWeight)*fullWidth;const matchWidth=(estimatedWidth/totalWeight)*fullWidth;try{if(Math.abs(angleDeg)>1){const offsetXRotated=offsetX*Math.cos(angleRad);const offsetYRotated=offsetX*Math.sin(angleRad);page.drawRectangle({x:x+offsetXRotated-1,y:y+offsetYRotated-2,width:matchWidth+2,height:textHeight+4,rotate:PDFLib.degrees(angleDeg),color:PDFLib.rgb(rgb.r/255,rgb.g/255,rgb.b/255),opacity:rule.opacity,borderWidth:0})}else{page.drawRectangle({x:x+offsetX-1,y:y-2,width:matchWidth+2,height:textHeight+4,color:PDFLib.rgb(rgb.r/255,rgb.g/255,rgb.b/255),opacity:rule.opacity,borderWidth:0})}}catch(drawError){console.error('Error drawing word highlight:',drawError)}startIndex+=matchLength}}addLog('info',`  Applied ${totalHighlights} highlights`);const pdfBytes=await pdfDoc.save();addLog('info',`  Generated PDF: ${formatFileSize(pdfBytes.length)}`);const pagesInfo=Array.from(pagesWithHighlights).sort((a,b)=>a-b).join(', ');return{pdfBytes:pdfBytes,highlightCount:totalHighlights,pagesInfo:pagesInfo,termDetails:Object.values(termStats)}}catch(error){console.error('PDF Processing Error:',error);throw new Error(`Failed to process PDF: ${error.message}`)}}function hexToRgb(hex){const result=/^#?([a-f\d]{2})([a-f\d]{2})([a-f\d]{2})$/i.exec(hex);return result ?{r:parseInt(result[1],16),g:parseInt(result[2],16),b:parseInt(result[3],16)}:{r:255,g:255,b:0}}function updateProgress(current,total){const percentage=(current/total)*100;document.getElementById('progressFill').style.width=percentage+'%';document.getElementById('progressText').textContent=`${current} / ${total} files processed`}function addLog(type,message){const log=document.getElementById('processingLog');const entry=document.createElement('div');entry.className=`log-entry log-${type}`;entry.textContent=`[${new Date().toLocaleTimeString()}] ${message}`;log.appendChild(entry);log.scrollTop=log.scrollHeight}function addDashboard(stats){const dashboardCard=document.getElementById('dashboardCard');dashboardCard.style.display='block';const dashboardContent=document.getElementById('dashboardContent');dashboardContent.innerHTML=`
                <div class="stat-card">
                    <div class="stat-icon">📁</div>
                    <div class="stat-value">${stats.totalFiles}</div>
                    <div class="stat-label">Total Files</div>
                </div>
                <div class="stat-card success">
                    <div class="stat-icon">✅</div>
                    <div class="stat-value">${stats.successfulFiles}</div>
                    <div class="stat-label">Successful</div>
                </div>
                ${stats.failedFiles > 0 ? `<div class="stat-card error"><div class="stat-icon">❌</div><div class="stat-value">${stats.failedFiles}</div><div class="stat-label">Failed</div></div>` : ''}
                <div class="stat-card">
                    <div class="stat-icon">📑</div>
                    <div class="stat-value">${stats.filesWithHighlights}</div>
                    <div class="stat-label">Files with Highlights</div>
                </div>
                <div class="stat-card highlight">
                    <div class="stat-icon">✨</div>
                    <div class="stat-value">${stats.totalHighlights}</div>
                    <div class="stat-label">Total Highlights</div>
                </div>
                <div class="stat-card">
                    <div class="stat-icon">⏱️</div>
                    <div class="stat-value">${stats.timeTaken}s</div>
                    <div class="stat-label">Time Taken</div>
                </div>
                <div class="stat-card" style="background: linear-gradient(135deg, rgba(255, 107, 0, 0.15), rgba(255, 153, 51, 0.15)); border: 2px solid rgba(255, 153, 51, 0.4);">
                    <div class="stat-icon">👤</div>
                    <div class="stat-value" style="color: #ffa64d; font-size: 1.8em;">${stats.lifetimeTotal}</div>
                    <div class="stat-label" style="color: #ffa64d;">Your Total Files</div>
                </div>
                <div class="stat-card" style="background: linear-gradient(135deg, rgba(0, 200, 83, 0.15), rgba(0, 255, 100, 0.15)); border: 2px solid rgba(0, 255, 100, 0.4);">
                    <div class="stat-icon">🌍</div>
                    <div class="stat-value" style="color: #00ff88; font-size: 1.8em;">${stats.globalTotal || 0}</div>
                    <div class="stat-label" style="color: #00ff88;">Global Total (All Users)</div>
                </div>
            `;let csvButtonContainer=document.getElementById('csvButtonContainer');if(!csvButtonContainer){csvButtonContainer=document.createElement('div');csvButtonContainer.id='csvButtonContainer';csvButtonContainer.style.textAlign='center';csvButtonContainer.style.marginTop='25px';csvButtonContainer.style.paddingTop='20px';csvButtonContainer.style.borderTop='1px solid rgba(255, 153, 51, 0.2)';dashboardCard.appendChild(csvButtonContainer)}csvButtonContainer.innerHTML=`
                <button id="exportCSVBtn" class="stat-card highlight" style="
                   background: rgba(255, 160, 51, 0.15);
                    box-shadow: 0 6px 25px rgba(255, 167, 51, 0.2);
                    padding: 10px 20px;
                    font-size: 12px;
                    font-weight: bold;
                    border-radius: 10px;
                    cursor: pointer;
                    border: 2px solid #ffcc00;
                    color: #ffcc00;
                    backdrop-filter: blur(10px);
                    transition: all 0.3s ease;
                    font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
                " onmouseover="this.style.transform='translateY(-3px)'; this.style.boxShadow='0 6px 25px rgba(255, 150, 51, 0.77)'"
                onmouseout="this.style.transform='translateY(0)'; this.style.boxShadow='0 4px 20px rgba(255, 147, 47, 1)'">
                    📊 Download Detail Excel Report
                </button>
            `;const exportBtn=document.getElementById('exportCSVBtn');if(exportBtn){exportBtn.addEventListener('click',exportDetailedCSV)}}async function downloadResults(){console.log('Download button clicked!');console.log('Processed files:',processedFiles.length);if(processedFiles.length===0){alert('No processed files to download');return}if(processedFiles.length===1){console.log('Downloading single PDF...');downloadSinglePDF(processedFiles[0]);return}addLog('info','Creating ZIP file...');console.log('Creating ZIP with',processedFiles.length,'files');try{const zip=new JSZip();const folder=zip.folder('highlighted_pdfs');processedFiles.forEach(file=>{const filename=file.name.replace('.pdf','_highlighted.pdf');console.log('Adding to ZIP:',filename);folder.file(filename,file.data)});console.log('Generating ZIP blob...');const content=await zip.generateAsync({type:'blob',compression:'DEFLATE',compressionOptions:{level:6}});console.log('ZIP generated, size:',content.size);const url=URL.createObjectURL(content);const a=document.createElement('a');a.href=url;a.download=`highlighted_pdfs_${Date.now()}.zip`;document.body.appendChild(a);console.log('Triggering download...');a.click();document.body.removeChild(a);setTimeout(()=>URL.revokeObjectURL(url),100);addLog('success','ZIP file downloaded successfully!')}catch(error){console.error('ZIP Error:',error);addLog('error',`Error creating ZIP: ${error.message}`);addLog('info','Attempting to download files individually...');downloadFilesIndividually()}}function downloadSinglePDF(file){try{console.log('Creating PDF blob for:',file.name);console.log('Data type:',typeof file.data,'Length:',file.data.length||file.data.byteLength);const blob=new Blob([file.data],{type:'application/pdf'});console.log('Blob created, size:',blob.size);const url=URL.createObjectURL(blob);const a=document.createElement('a');a.href=url;a.download=file.name.replace('.pdf','_highlighted.pdf');console.log('Download filename:',a.download);document.body.appendChild(a);a.click();console.log('Click triggered');document.body.removeChild(a);setTimeout(()=>URL.revokeObjectURL(url),100);addLog('success',`Downloaded: ${a.download}`)}catch(error){console.error('Download Error:',error);addLog('error',`Failed to download: ${error.message}`)}}function downloadFilesIndividually(){processedFiles.forEach((file,index)=>{setTimeout(()=>{downloadSinglePDF(file)},index*500)})}document.addEventListener('DOMContentLoaded',async()=>{loadSavedRules();loadLifetimeCount();await fetchGlobalCount();displayRules();displayFiles();updateProcessButton();initDragAndDrop();document.getElementById('addRuleBtn').addEventListener('click',addRule);document.getElementById('cancelEditBtn').addEventListener('click',cancelEdit);document.getElementById('clearAllBtn').addEventListener('click',clearAllRules);document.getElementById('exportRulesBtn').addEventListener('click',exportRules);document.getElementById('importRulesFile').addEventListener('change',importRules);document.getElementById('processBtn').addEventListener('click',processFiles);document.getElementById('downloadBtn').addEventListener('click',downloadResults);document.getElementById('pdfFiles').addEventListener('change',handlePDFUpload);document.getElementById('zipFile').addEventListener('change',handleZIPUpload);document.getElementById('clearAllFilesBtn').addEventListener('click',clearAllFiles);document.getElementById('addRuleBtn').addEventListener('click',addRule);document.getElementById('wordInput').addEventListener('keypress',(e)=>{if(e.key==='Enter'){addRule()}})});function initDragAndDrop(){const pdfLabel=document.querySelector('label[for="pdfFiles"]');const zipLabel=document.querySelector('label[for="zipFile"]');['dragenter','dragover','dragleave','drop'].forEach(eventName=>{document.body.addEventListener(eventName,preventDefaults,false)});function preventDefaults(e){e.preventDefault();e.stopPropagation()}['dragenter','dragover'].forEach(eventName=>{pdfLabel.addEventListener(eventName,()=>{pdfLabel.classList.add('drag-over')},false)});['dragleave','drop'].forEach(eventName=>{pdfLabel.addEventListener(eventName,()=>{pdfLabel.classList.remove('drag-over')},false)});pdfLabel.addEventListener('drop',handlePDFDrop,false);['dragenter','dragover'].forEach(eventName=>{zipLabel.addEventListener(eventName,()=>{zipLabel.classList.add('drag-over')},false)});['dragleave','drop'].forEach(eventName=>{zipLabel.addEventListener(eventName,()=>{zipLabel.classList.remove('drag-over')},false)});zipLabel.addEventListener('drop',handleZIPDrop,false)}function handlePDFDrop(e){const dt=e.dataTransfer;const files=Array.from(dt.files);console.log('PDF files dropped:',files.length);let addedCount=0;files.forEach(file=>{console.log('Checking dropped file:',file.name,'Type:',file.type);if(file.type==='application/pdf'){const isDuplicate=uploadedFiles.some(existingFile=>existingFile.name===file.name&&existingFile.size===file.size);if(!isDuplicate){uploadedFiles.push(file);addedCount++;console.log('Added dropped file:',file.name)}else{console.log('Duplicate dropped file:',file.name)}}else{console.log('Not a PDF, skipping:',file.name);alert(`${file.name} is not a PDF file`)}});console.log(`Total added: ${addedCount}, Total files now: ${uploadedFiles.length}`);displayFiles();updateProcessButton()}async function handleZIPDrop(e){const dt=e.dataTransfer;const files=Array.from(dt.files);if(files.length===0)return;const file=files[0];if(!file.name.toLowerCase().endsWith('.zip')){alert('Please drop a ZIP file');return}console.log('ZIP file dropped:',file.name);try{const zip=await JSZip.loadAsync(file);let pdfCount=0;let duplicateCount=0;for(const [filename,zipEntry] of Object.entries(zip.files)){if(!zipEntry.dir&&filename.toLowerCase().endsWith('.pdf')){const blob=await zipEntry.async('blob');const pdfFile=new File([blob],filename,{type:'application/pdf'});console.log('Checking ZIP file:',pdfFile.name,'Size:',pdfFile.size);const isDuplicate=uploadedFiles.some(existingFile=>existingFile.name===pdfFile.name&&existingFile.size===pdfFile.size);if(!isDuplicate){uploadedFiles.push(pdfFile);pdfCount++;console.log('Added from ZIP:',pdfFile.name)}else{duplicateCount++;console.log('Duplicate in ZIP:',pdfFile.name)}}}console.log(`Extracted ${pdfCount} PDFs, skipped ${duplicateCount} duplicates. Total files: ${uploadedFiles.length}`);if(pdfCount>0){alert(`Successfully extracted ${pdfCount} PDF file(s) from ZIP!`)}if(duplicateCount>0){alert(`Skipped ${duplicateCount} duplicate file(s)`)}displayFiles();updateProcessButton()}catch(error){console.error('Error reading dropped ZIP file:',error);alert('Error reading ZIP file. Please ensure it contains valid PDF files.')}}const canvas=document.getElementById('particleCanvas');const ctx=canvas.getContext('2d');function resizeCanvas(){canvas.width=window.innerWidth;canvas.height=window.innerHeight}resizeCanvas();window.addEventListener('resize',resizeCanvas);const mouse={x:window.innerWidth/2,y:window.innerHeight/2,radius:150};window.addEventListener('mousemove',(e)=>{mouse.x=e.x;mouse.y=e.y});class Particle{constructor(){this.x=Math.random()*canvas.width;this.y=Math.random()*canvas.height;this.size=Math.random()*3+1;this.baseSize=this.size;this.speedX=(Math.random()-0.5)*0.5;this.speedY=(Math.random()-0.5)*0.5;this.color=this.getRandomColor();this.pulseSpeed=Math.random()*0.02+0.01;this.pulsePhase=Math.random()*Math.PI*2}getRandomColor(){const colors=[ 'rgba(255, 107, 0, 0.6)','rgba(255, 153, 51, 0.6)','rgba(255, 204, 0, 0.6)','rgba(255, 122, 26, 0.5)' ];return colors[Math.floor(Math.random()*colors.length)]}update(){this.x+=this.speedX;this.y+=this.speedY;if(this.x>canvas.width)this.x=0;if(this.x<0)this.x=canvas.width;if(this.y>canvas.height)this.y=0;if(this.y<0)this.y=canvas.height;this.pulsePhase+=this.pulseSpeed;this.size=this.baseSize+Math.sin(this.pulsePhase)*1.5;const dx=mouse.x-this.x;const dy=mouse.y-this.y;const distance=Math.sqrt(dx*dx+dy*dy);if(distance<mouse.radius){const force=(mouse.radius-distance)/mouse.radius;const angle=Math.atan2(dy,dx);this.x+=Math.cos(angle)*force*2;this.y+=Math.sin(angle)*force*2}}draw(){const drawSize=Math.abs(this.size);if(drawSize<0.1)return;ctx.beginPath();ctx.arc(this.x,this.y,drawSize,0,Math.PI*2);ctx.fillStyle=this.color;ctx.shadowBlur=10;ctx.shadowColor=this.color;ctx.fill();ctx.shadowBlur=0}}const particles=[];const particleCount=100;for(let i=0;i<particleCount;i++){particles.push(new Particle())}function connectParticles(){for(let i=0;i<particles.length;i++){for(let j=i+1;j<particles.length;j++){const dx=particles[i].x-particles[j].x;const dy=particles[i].y-particles[j].y;const distance=Math.sqrt(dx*dx+dy*dy);if(distance<100){const opacity=(1-distance/100)*0.3;ctx.strokeStyle=`rgba(255, 153, 51, ${opacity})`;ctx.lineWidth=1;ctx.beginPath();ctx.moveTo(particles[i].x,particles[i].y);ctx.lineTo(particles[j].x,particles[j].y);ctx.stroke()}}}}function animate(){ctx.fillStyle='rgba(42, 21, 6, 0.1)';ctx.fillRect(0,0,canvas.width,canvas.height);particles.forEach(particle=>{particle.update();particle.draw()});connectParticles();requestAnimationFrame(animate)}animate();async function exportDetailedCSV(){if(detailedReport.length===0){alert('No data to export. Please process files first.');return}try{if(typeof ExcelJS==='undefined'){const script=document.createElement('script');script.src='https://cdn.jsdelivr.net/npm/exceljs@4.3.0/dist/exceljs.min.js';document.head.appendChild(script);await new Promise(resolve=>script.onload=resolve)}const workbook=new ExcelJS.Workbook();const worksheet=workbook.addWorksheet('Highlight Report');worksheet.columns=[{header:'Filename',key:'filename',width:40},{header:'Term',key:'term',width:30},{header:'Pages',key:'pages',width:15},{header:'Total Count',key:'count',width:12}];const headerRow=worksheet.getRow(1);for(let col=1;col<=4;col++){const cell=headerRow.getCell(col);cell.font={bold:true,color:{argb:'FFFFFFFF'},size:11};cell.fill={type:'pattern',pattern:'solid',fgColor:{argb:'FFFF6B00'}};cell.alignment={horizontal:'center',vertical:'middle'}}const fileRows=[];detailedReport.forEach(fileReport=>{const filename=fileReport.filename;const termsInFile=[];fileReport.termDetails.forEach(termDetail=>{if(termDetail.count>0){termsInFile.push({term:termDetail.term,color:termDetail.color,pages:termDetail.pages.join(','),count:termDetail.count})}});if(termsInFile.length>0){const allPages=[...new Set(termsInFile.flatMap(t=>t.pages.split(',')))].sort((a,b)=>parseInt(a)-parseInt(b)).join(',');const totalCount=termsInFile.reduce((sum,t)=>sum+t.count,0);fileRows.push({filename:filename,terms:termsInFile,pages:allPages,totalCount:totalCount})}});fileRows.forEach(row=>{const newRow=worksheet.addRow({filename:row.filename,term:row.terms.map(t=>t.term).join(';'),pages:row.pages,count:row.totalCount});const termCell=newRow.getCell(2);const richText=[];row.terms.forEach((termData,idx)=>{richText.push({text:termData.term,font:{color:{argb:'FF'+termData.color.replace('#','')},bold:true,size:11}});if(idx<row.terms.length-1){richText.push({text:';',font:{color:{argb:'FFFFFFFF'},bold:true,size:11}})}});termCell.value={richText:richText};termCell.fill={type:'pattern',pattern:'solid',fgColor:{argb:'FF000000'}};termCell.alignment={vertical:'middle',horizontal:'left'}});let grandTotal=0;detailedReport.forEach(fileReport=>{const totalHighlightsInFile=fileReport.termDetails.reduce((sum,term)=>sum+term.count,0);if(totalHighlightsInFile===0){const nullRow=worksheet.addRow({filename:fileReport.filename,term:'NULL',pages:'NULL',count:0});nullRow.eachCell((cell)=>{cell.font={color:{argb:'FFFF0000'},bold:true,size:11};cell.alignment={vertical:'middle',horizontal:'left'}})}});grandTotal=fileRows.reduce((sum,row)=>sum+row.totalCount,0);const totalRow=worksheet.addRow({filename:'',term:'',pages:'',count:`Total ${grandTotal}`});const totalCell=totalRow.getCell(4);totalCell.font={bold:true,size:14,color:{argb:'FF000000'}};totalCell.alignment={vertical:'middle',horizontal:'left'};const buffer=await workbook.xlsx.writeBuffer();const blob=new Blob([buffer],{type:'application/vnd.openxmlformats-officedocument.spreadsheetml.sheet'});const url=URL.createObjectURL(blob);const link=document.createElement('a');link.href=url;link.download=`pdf-highlighter-report-${Date.now()}.xlsx`;document.body.appendChild(link);link.click();document.body.removeChild(link);URL.revokeObjectURL(url);const exportBtn=document.getElementById('exportCSVBtn');const originalText=exportBtn.textContent;exportBtn.textContent='✓ Excel Downloaded!';exportBtn.style.background='linear-gradient(135deg, #00aa00, #00dd00)';setTimeout(()=>{exportBtn.textContent=originalText;exportBtn.style.background='linear-gradient(135deg, rgba(255, 160, 51, 0.15), rgba(255, 160, 51, 0.15))'},9000);console.log('Excel report exported successfully!')}catch(error){console.error('Error exporting Excel:',error);alert('Error creating Excel file. Please try again.')}}
})(window,document);
